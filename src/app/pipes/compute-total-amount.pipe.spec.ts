import { ComputeTotalAmountPipe } from './compute-total-amount.pipe';

describe('ComputeTotalAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ComputeTotalAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
